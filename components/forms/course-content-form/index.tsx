import React from "react"

type Props = {
  groupId: string
  sectionId: string
  userId: string
}

const CourseContentForm = ({ groupId, sectionId, userId }: Props) => {
  return <div>CourseContentForm</div>
}

export default CourseContentForm
